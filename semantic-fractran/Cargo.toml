[package]
name = "semantic-fractran"
version = "0.1.0"
edition = "2021"
description = "Universal SemanticFractran trait — state as Gödel numbers, actions as prime ratios"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
ciborium = "0.2"
